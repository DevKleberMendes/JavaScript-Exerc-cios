// for (estrutura de repetição - loop)
// for similar ao whily porém for mais usado

for(var i =0; i < 10; i++) {
    console.log("Repetindo for: " + i);

}

// obs  i++ (sig i+1)



// Outro exemplo de for em array
// variável + condição e + atribuição (nesta ordem)
var arr = [1,2,3,4];

for(var j = 0; j < arr.length; j++) {

}

console.log(arr.length);

// obs length ( cumprimento do arr = 4 itens)


// outro exempo de for
for(var x = 5; x < 100; x *=3) {
    console.log(x);
}